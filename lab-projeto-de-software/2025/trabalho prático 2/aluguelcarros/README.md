# Aluguel de Carros — Código do Projeto (TP2)

Código fonte do **Sistema de Aluguel de Carros** (Trabalho Prático 2). A documentação da estrutura, camadas e como executar está no **README do trabalho prático 2**: `../README.md` (um nível acima).

**Resumo:** API REST Spring Boot (clientes, empresas, automóveis, pedidos, contratos, etc.). Execute com `mvn spring-boot:run` nesta pasta (`aluguelcarros`). Documentação Swagger em `/swagger-ui.html`.
